# 联合查询

执行下面的 SQL 语句，直接合并查询结果。

```sql
SELECT * FROM USER WHERE dept_id = 1 UNION ALL SELECT * FROM USER WHERE dept_id = 3;
```

执行下面的 SQL 语句，合并后自动去重查询结果。

```sql
SELECT * FROM USER WHERE dept_id = 1 UNION SELECT * FROM USER WHERE dept_id = 3;
```

