# 数值函数

执行下面的 SQL 语句，对数值向上取整。

```sql
SELECT CEIL(2.33);
```

执行下面的 SQL 语句，对数值向下取整。

```sql
SELECT FLOOR(2.33);
```

执行下面的 SQL 语句，返回两个数相除后的余数。

```sql
SELECT MOD(8, 3);
```

执行下面的 SQL 语句，生成 0 到 1 之间的随机浮点数。

```sql
SELECT RAND();
```

执行下面的 SQL 语句，对数值进行四舍五入。

```sql
SELECT ROUND(3.14);
```

