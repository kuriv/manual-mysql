# 事务操作

执行下面的 SQL 语句，查看事务提交方式。

```sql
SELECT @@AUTOCOMMIT;
```

执行下面的 SQL 语句，设置事务提交方式。

```sql
```

执行下面的 SQL 语句，查看事务隔离级别。

```sql
SELECT @@TRANSACTION_ISOLATION;
```

执行下面的 SQL 语句，设置事务隔离级别。

```sql
```

执行下面的 SQL 语句，开启事务。

```sql
```

执行下面的 SQL 语句，提交事务。

```sql
```

执行下面的 SQL 语句，回滚事务。

```sql
```

