# 修改数据

执行下面的 SQL 语句，修改指定条件的数据。

```sql
UPDATE user_login
SET password = '******'
WHERE username = 'Tom';

UPDATE user_login
SET password    = '000000',
    create_time = CURRENT_TIMESTAMP
WHERE username = 'Spike';
```
